function add(x, y) {
  if (typeof x !== "number");
}

// 12.5.3 매개변수의 최대개수 (이상적인 함수는 한 가지 일만 해야 하며 가급적 작게 만들어야 한다.)
// jQuery의 ajax 메서드에 객체를 인수로 전달하는 예

$.ajax({
  method: "POST",
  url: "/12_함수",
  data: { id: 1, naem: "Lee" },
  cache: false,
});
// https://api.jquery.com/jquery.ajax
